#ifndef _CONFIG_H_
#define _CONFIG_H_

/// All motors must be declared in this fashion in main.h, copy/paste these sensors into main.h as well
/// all of the sensors are needed for the ai system to run.

/*
// Drive motors
extern Motor lFront;
extern Motor lBack;
extern Motor rFront;
extern Motor rBack;

// Intake motors
extern Motor lIntake;
extern Motor rIntake;
extern Motor internalIntake;
extern Motor pooperIntake;

// Sensors
  // Intake-related
extern Optical opticalSort;
extern ADIAnalogIn towerSens;
extern ADIAnalogIn midBallSens;
extern ADIAnalogIn frontBallSens;
  // Drive-related
extern Distance towerDist;
extern Imu driveInert;
extern Vision driveVis;
*/

#endif
